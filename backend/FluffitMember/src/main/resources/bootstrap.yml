spring:
  cloud:
    config:
      uri: https://fluffit.shop:8888 #http://127.0.0.1:8888
      name: member-service
  #Profiles을 사용한 Configuration 적용
  #.yml 파일명은 user-service-dev로 설정
#  profiles:
#    active: dev

